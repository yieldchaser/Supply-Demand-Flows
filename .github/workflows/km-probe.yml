name: KM Probe 2
on:
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: recon/sabine-corpus
      - name: List recon branch files
        run: |
          git ls-tree -r --name-only HEAD | head -40
